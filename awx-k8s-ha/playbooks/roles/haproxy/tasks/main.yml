- name: Render haproxy config
  template:
    src: haproxy.cfg.j2
    dest: /etc/haproxy/haproxy.cfg
    mode: '0644'
  notify: restart haproxy

- name: Enable haproxy
  service:
    name: haproxy
    state: started
    enabled: true

